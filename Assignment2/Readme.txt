ASSIGNMENT 2-README.TXT

Elements used:
1. header:The <header> element represents a container for introductory content or a set of navigational links.
2. footer:The <footer> tag defines a footer for a document or section.
3. favicon:A favicon image is displayed to the left of the page title in the browser tab
4. section:The <section> tag defines a section in a document.
5. div:The <div> tag is used as a container for HTML elements - which is then styled with CSS or manipulated with JavaScript.
6. nav:The <nav> tag defines a set of navigation links.
7. ul&li:The <ul> tag defines an unordered list.
Use the <ul> tag together with the <li> tag to create unordered lists.
8. anchor:The <a> tag defines a hyperlink, which is used to link from one page to another.
9. heading (h1-h5):HTML headings are titles or subtitles that you want to display on a webpage.
10. paragraph:The <p> tag defines a paragraph of text. It is a block-level element and always starts on a new line.
11. br:The <br> tag in HTML document is used to create a line break in a text.
12. table, tr,th,td: The <table> tag defines an HTML table.
An HTML table consists of one <table> element and one or more <tr>, <th>, and <td> elements.
The <tr> element defines a table row, the <th> element defines a table header, and the <td> element defines a table cell.
13. image:The HTML <img> tag is used to embed an image in a web page.
14. figure:The <figure> tag specifies self-contained content, like illustrations, diagrams, photos, code listings, etc.
15. figcaption:The <figcaption> HTML element represents a caption or legend describing the rest of the contents of its parent <figure> element.

PROPERTIES USED:
1. Scroll behaviour
2. padding
3. margins
4. display
5. font family/size
6. width
7. overflow
8. height
9. flex wrap
10. position:absolute/relative
11. text decoration
12. list style
13. webkit extensions 
14. @media queries for responsive behaviour of the website.
15. border
16. float
17. transform
18. transition
19. text-align
20. background/text colors
21. box sizing


Coloumn structure layout with float/overflow and absolute/relative properties where tel/mailto 
is used to provide contact information and figcaptions with hover effects to display 
certifications in portfolio is designed.

Creative aspects:
1. CSS animation property used to create cool design to display name. 
2. Creative usage of icons that upon clicked directs to new page. 
3. Projects, resumes, certifications linked in a way that when clicked, respective content shows up.
4. Up arrow icon used in footer to navigate the page from bottom to top of the page.

